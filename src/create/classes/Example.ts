class Example {
    x: number
    y: number
    label: string
    color: string

    constructor(x: number, y: number, label: string, color: string) {
        this.x = x
        this.y = y
        this.label = label
        this.color = color
    }
}
